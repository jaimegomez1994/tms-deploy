export const regexLettersNumbersSpaces = /^[a-zA-Z0-9\s]*$/;

export const regexOnlyLettersRegex = /^[a-zA-Z]+$/;

export const regexOnlyNumbersRegex = /^\d+$/;

export const regexLettersNumbers = /^[a-zA-Z0-9]+$/;

export const regexLettersSpaces = /^[a-zA-Z\s]+$/;

export const regexDecimal = /^\d+(\.\d+)?\.?$|^\.?\d+$/;
