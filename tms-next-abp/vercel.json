{
    "version": 2,
    "builds": [
        {
            "src": "apps/abp/package.json",
            "use": "@vercel/next"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "apps/abp/$1"
        }
    ]
}
